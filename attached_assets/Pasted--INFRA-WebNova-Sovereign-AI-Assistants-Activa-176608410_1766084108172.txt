📩 التوجيه التنفيذي الحاسم
INFRA WebNova – تفعيل المساعدين السياديين (Sovereign AI Assistants Activation Directive)
🚨 الحالة الحرجة الحالية (Critical Issue)

المساعدون السياديون ظاهرون في الواجهة فقط

لا يوجد:

Execution Engine

Model Binding

Command Routing

Task Lifecycle

زر "إرسال أمر" لا ينفذ شيئًا فعليًا

هذا يجعل المنصة غير صالحة للإنتاج

❌ هذا خلل بنيوي وليس UI Bug

🎯 الهدف الإلزامي (Non-Negotiable)

تحويل كل مساعد سيادي من:

🖼️ Icon + Card
إلى
🤖 AI Agent حيّ قادر على استقبال أوامر وتنفيذها وتتبع نتائجها

🧠 1️⃣ المبدأ الحاكم (Mandatory Principle)

كل مساعد = كيان تنفيذي مستقل (Executable AI Agent)
وليس Component واجهة فقط

🏗️ 2️⃣ المعمارية الإلزامية لتفعيل المساعدين
User Command
   ↓
Assistant Controller
   ↓
Agent Execution Engine
   ↓
Model Router (AUTO / MANUAL)
   ↓
Provider API (OpenAI / Claude / Gemini / DeepSeek / etc.)
   ↓
Response Processor
   ↓
Usage & Cost Tracker
   ↓
UI Update + Logs

🤖 3️⃣ تعريف المساعد السيادي (MANDATORY)
interface SovereignAssistant {
  id: string;
  name: string;
  role: string;

  systemPrompt: string; // شخصية المساعد

  allowedModels: AIModel[];
  defaultModel: AIModel;

  executionMode: 'AUTO' | 'MANUAL';

  permissions: string[];
  restrictions: string[];

  isEnabled: boolean;

  execute(command: AssistantCommand): Promise<AssistantResult>;
}


❌ ممنوع وجود مساعد بدون execute()

⚙️ 4️⃣ محرك التنفيذ (Assistant Execution Engine)
يجب إنشاء ملف/خدمة:
/core/ai/assistant-engine.ts

class AssistantEngine {
  async runAssistant(
    assistantId: string,
    command: string,
    context: ExecutionContext
  ) {
    const assistant = await this.loadAssistant(assistantId);

    if (!assistant.isEnabled) {
      throw new Error("Assistant disabled");
    }

    const model = await this.selectModel(assistant, context);

    const response = await this.callModel({
      model,
      systemPrompt: assistant.systemPrompt,
      userPrompt: command
    });

    await this.logUsage(assistant, model, response);

    return response;
  }
}

🧠 5️⃣ ربط زر “إرسال أمر” (CRITICAL FIX)
كل زر في الواجهة يجب أن:

يرسل assistantId

يرسل command

يستدعي Endpoint فعلي

API Endpoint إلزامي:
POST /api/assistants/{assistantId}/execute

POST /execute
{
  "command": "حلل أداء المنصة خلال 30 يوم",
  "mode": "AUTO"
}

🔀 6️⃣ اختيار النموذج (AUTO / MANUAL)
AUTO:

النظام يختار النموذج الأنسب حسب:

نوع المهمة

التكلفة

الأداء

MANUAL:

المستخدم يحدد:

GPT-4 / Claude / Gemini / DeepSeek

الحد الأقصى للتكلفة

🧩 7️⃣ تشغيل مساعد واحد أو عدة مساعدين
executionType: 'SINGLE' | 'MULTI'


Sequential

Parallel

مثال:

استراتيجي → محلل → أمني

💰 8️⃣ تتبع الاستهلاك (Mandatory)

كل أمر يجب أن يولّد سجل:

AssistantUsage {
  assistantId
  model
  tokens
  realCost
  chargedCost
  userId
  timestamp
}


ويُعرض:

أمام المالك

أمام المستخدم (حسب الصلاحيات)

🛑 9️⃣ زر إيقاف فوري (Kill Switch)
لكل مساعد:

⏸️ إيقاف مؤقت

❌ تعطيل كامل

يمنع:

أي تنفيذ

أي استدعاء API

📜 🔟 سجل الأوامر (Task History)

لكل مساعد:

الأوامر

النتائج

النموذج

الوقت

التكلفة

إعادة التشغيل

🧪 11️⃣ اختبار القبول (Acceptance Test)

يُعتبر التنفيذ ناجحًا فقط إذا:

✅ عند كتابة أمر → يظهر رد فعلي

✅ يظهر النموذج المستخدم

✅ تُسجّل التكلفة

✅ يمكن تعطيل المساعد

✅ لا يوجد أي مساعد بدون Execution

🏁 الخلاصة التنفيذية

المساعدون السياديون يجب أن يصبحوا Agents حقيقيين لا UI Cards

بدون تنفيذ هذا التوجيه:

المنصة غير قابلة للنشر

الذكاء الاصطناعي شكلي فقط

المخاطر عالية

بعد التنفيذ:

كل مساعد يعمل بكفاءة

كل أمر يُنفذ

كل تكلفة محسوبة

المالك يملك سيطرة سيادية كاملة