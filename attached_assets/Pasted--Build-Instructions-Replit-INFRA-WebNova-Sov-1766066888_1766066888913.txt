هذا أمر تنفيذي ملزم (Build Instructions)

📩 التوجيه التنفيذي النهائي إلى Replit
INFRA WebNova – Sovereign API Keys System (Production Final)
الدور المطلوب من Replit

أنت مسؤول عن تنفيذ نظام مفاتيح API متكامل وجاهز للإنتاج داخل منصة INFRA WebNova، بدون افتراض أي تطوير لاحق بعد النشر.

سيتم نشر المنصة على Hetzner Dedicated Server
ولا يُسمح بالاعتماد على تطوير مستقبلي لتعويض نقص في البنية.

🎯 الهدف الاستراتيجي

إنشاء نظام API Keys سيادي (Sovereign):

آمن

معزول بين المنصات (Multi-Tenant)

قابل للتوسع عبر الإعدادات فقط (Config-Driven)

مطابق لمعايير AWS / Stripe / Vercel / Supabase

يعمل سنوات بدون تعديل كود

1️⃣ المتطلبات غير القابلة للتفاوض (MANDATORY)
1. نظام API Keys كامل

دعم عدة مفاتيح لكل منصة

كل مفتاح يحتوي:

اسم

Prefix تعريفي

Scopes (صلاحيات)

Rate Limits

تاريخ إنشاء / آخر استخدام

حالة (Active / Revoked / Expired)

لا يوجد أي مفتاح بصلاحيات كاملة افتراضيًا

عرض المفتاح مرة واحدة فقط عند الإنشاء

2. ربط حصري بالمنصة (Tenant Binding)

كل API Key مرتبط بـ منصة واحدة فقط

أي طلب:

يُرفض فورًا إذا لم يتطابق المفتاح مع المنصة

منع أي Cross-Tenant Access بشكل صارم

3. نظام Scopes إلزامي

تنفيذ نظام Scopes منذ البداية، حتى لو لم تُستخدم كلها الآن.

أمثلة Scopes (قابلة للتوسع):

platform.read / platform.write

domains.read / domains.manage

ai.invoke

billing.read / billing.manage

api_keys.read / api_keys.manage

webhooks.send

⚠️ يمنع استخدام * أو all

4️⃣ المصادقة (Authentication)

Header:

Authorization: Bearer <API_KEY>


التحقق الإجباري:

المفتاح موجود

غير موقوف

غير منتهي

Scopes صحيحة

تابع للمنصة الصحيحة

5️⃣ Rate Limiting & Abuse Protection

حدود متعددة:

دقيقة / ساعة / يوم

Burst Protection

سياسات متدرجة:

تحذير

رفض

تعليق مؤقت

تسجيل أي تجاوز أو سلوك مشبوه

6️⃣ Audit Logs (إلزامي)

تسجيل كل شيء:

استخدام API

فشل مصادقة

إنشاء / إلغاء / تدوير مفاتيح

السجلات:

Immutable (غير قابلة للتعديل)

Owner-Only

تشمل IP / Timestamp / Scope / Endpoint

7️⃣ واجهة الإدارة (Owner-Only UI)

إنشاء قسم جديد في لوحة المالك باسم:

🔑 مفاتيح API

يشمل:

جدول المفاتيح

الصلاحيات

الحالة

آخر استخدام

أزرار:

Create

Revoke

Rotate

Delete

تحذير أمني دائم:

⚠️ هذا المفتاح يمنح وصولًا مباشرًا للمنصة – لا تشاركه

8️⃣ Webhooks (تأسيس من الآن)

حتى لو لم تُستخدم فورًا:

دعم:

Endpoint خارجي

Secret Key

توقيع HMAC

أحداث جاهزة:

DOMAIN_VERIFIED

SSL_ISSUED

PLATFORM_STATUS_CHANGED

API_KEY_CREATED / REVOKED

9️⃣ Config-Driven Design (أساسي)

كل ما يلي يجب أن يكون قابل للتعديل من UI:

Rate Limits

Scopes

سياسات الأمان

Webhooks

تعطيل API لمنصة كاملة

❌ لا Hard-Code لسياسات أساسية

🔐 الأمان (Non-Negotiable)

تخزين المفاتيح: HASH ONLY (Argon2 / bcrypt)

HTTPS إجباري

CORS مضبوط

إمكانية تعطيل API بالكامل لمنصة واحدة فورًا

📦 متطلبات التسليم (DELIVERABLES)

نظام API Keys مكتمل

Middleware للتحقق

UI للمالك

Webhooks جاهزة

توثيق داخلي (أمثلة Request فقط)

بدون Developer Portal عامة

🏁 تعريف النجاح

يمكن ربط أي خدمة خارجية بدون تعديل كود

يمكن إيقاف أي تكامل فورًا

لا حاجة لأي تطوير بعد النشر

جاهز للعمل سنوات على Hetzner

✅ خلاصة تنفيذية

نفّذ هذا النظام كمنتج نهائي (Final Production System)
وليس كنموذج أولي أو مرحلة تجريبية.