🚨 توجيه صارم (Technical Governance Policy)

ملزم التطبيق على كل صفحات المنصة – بدون استثناء

1️⃣ حدود قصوى لعدد الأسطر (NON-NEGOTIABLE)

❌ ممنوع تمامًا:

أي ملف UI يتعدّى 300 سطر

أي صفحة (Page / Route) تتعدّى 400 سطر

أي Hook يتعدّى 200 سطر

أي Service / API file يتعدّى 250 سطر

أي Utility file يتعدّى 150 سطر

✔️ لو الملف قرب يوصل للحد → يتقسم فورًا
بدون نقاش أو تأجيل.

أي ملف يوصل 80% من الحد يعتبر إنذار مبكر.

2️⃣ قاعدة “الصفحة = مُنسّق فقط”

📌 الصفحة لا تحتوي منطق أعمال.

الصفحة مسموح لها فقط:

Composition

Routing

Layout

تمرير props

❌ ممنوع داخل الصفحة:

API calls

Loops تقيلة

Calculations

Data transformation

Business logic

كل ده يطلع إلى:

hooks/

services/

utils/

3️⃣ فصل صارم للمسؤوليات (Separation of Concerns)
الهيكل الإجباري:
pages/
 └── SovereignPage.tsx      (≤ 400 lines)

components/
 ├── SovereignHeader.tsx    (≤ 300)
 ├── SovereignTable.tsx
 ├── SovereignMap.tsx

hooks/
 └── useSovereign.ts        (≤ 200)

services/
 └── sovereign.service.ts  (≤ 250)

utils/
 └── sovereign.utils.ts    (≤ 150)


أي خرق = رفض Pull Request.

4️⃣ Lazy Loading إجباري لكل صفحة ثقيلة

📌 أي صفحة فيها:

Charts

Maps

Tables كبيرة

Heavy UI

✔️ لا تُحمّل إلا عند الطلب

const SovereignMap = lazy(() => import('./SovereignMap'));


❌ تحميل مباشر = خطأ معماري.

5️⃣ أي جدول أو قائمة > 100 عنصر = Virtualization

❌ ممنوع render مباشر

✔️ إلزامي:

react-window

react-virtualized

6️⃣ قياس الأداء قبل وبعد (Mandatory)

أي صفحة لازم تحقق:

⏱ First Load < 2.5s

🧠 JS execution < 1s

🔁 Re-render أقل من 3 مرات عند التفاعل

لو فشلت → لا تُنشر

7️⃣ مراجعة شاملة لكل المنصة (Immediate Action)
🔍 خطوات إلزامية:

Scan كل الملفات

أي ملف > الحد → refactor

استخراج:

Business logic من الصفحات

API calls من components

تقسيم الصفحات الكبيرة

إضافة lazy loading

إضافة memoization

اختبار performance

8️⃣ قاعدة ذهبية (لا تُكسر)

لو المنصة بطيئة بدون مشتركين
هتنهار مع أول 10 مشتركين

اللي حصل ده تحذير مبكر مش مشكلة عابرة.

9️⃣ قرار إداري (مهم)

❗ استمرار كتابة ملفات ضخمة:

إهدار وقت

تكلفة مستقبلية

تعطيل المنصة بالكامل

التنظيف دلوقتي أرخص ×10 من الإصلاح بعد الإطلاق

10️⃣ التوجيه النهائي (مختصر)

✔️ لا ملف ضخم
✔️ لا صفحة ذكية
✔️ لا تحميل غير كسول
✔️ لا جداول بدون virtualization
✔️ لا merge بدون احترام الحدود