# مواصفات واجهة المستخدم وتجربة الاستخدام (UI/UX Specification)

## 1. فلسفة التصميم (Design Philosophy)
- **الهوية البصرية:** تصميم بسيط (Minimalist) يركز على الكود، يدعم الوضع الداكن (Dark Mode) افتراضياً لراحة العين أثناء البرمجة الطويلة.
- **المرونة (Fluidity):** واجهة تعتمد على الألواح القابلة للتغيير والحجم (Resizable Panes) وتدعم السحب والإفلات (Drag & Drop) لتخصيص مساحة العمل.

## 2. واجهة سطح المكتب (Desktop Interface Layout)
تتكون الشاشة الرئيسية من ثلاثة أعمدة رئيسية (3-Column Layout):

### أ. العمود الأيسر: شريط الأدوات والملفات (Sidebar & Tools Dock)
- **مستكشف الملفات (File Tree):** عرض هرمي لملفات ومجلدات المشروع مع أيقونات تميز أنواع الملفات (ts, js, py, css).
- **قائمة الأدوات (Tools Dock):** شريط عمودي ضيق يحتوي على أيقونات الوصول السريع:
  - 🛠 **الأدوات (Tools):** للوصول لكافة أدوات النظام.
  - 🔍 **البحث (Search):** بحث شامل داخل الملفات (Regex support).
  - 📦 **الحزم (Packages):** مدير حزم رسومي (GUI Wrapper) لـ NPM/Pip.
  - 🔒 **الأسرار (Secrets):** واجهة آمنة لإدارة متغيرات البيئة (Environment Variables).
  - 🗄 **قاعدة البيانات (Database):** واجهة بصرية لعرض وتعديل جداول قواعد البيانات (PostgreSQL).

### ب. العمود الأوسط: المحرر (Code Editor)
- **المحرك:** مبني على تقنيات مثل Monaco Editor (المستخدم في VS Code).
- **الميزات:**
  - تلوين الكود (Syntax Highlighting) ذكي.
  - إكمال تلقائي (IntelliSense) مدعوم بالذكاء الاصطناعي.
  - دعم تعدد المؤشرات (Multi-cursor editing).
  - علامات التبويب (Tabs) لفتح ملفات متعددة في آن واحد.

### ج. العمود الأيمن: المعاينة والتشغيل (Preview & Runtime)
- **الطرفية (Shell/Console):** نافذة لتنفيذ الأوامر وعرض مخرجات السيرفر (Logs).
- **المعاينة (Webview):** متصفح مدمج (Embedded Browser) يعرض التطبيق الناتج مباشرة (Hot Reloading) مع أدوات المطور (DevTools).

## 3. أدوات النظام الأساسية (Core Tooling)
يجب أن تحتوي المنظومة على الأدوات التالية مدمجة في الواجهة:
1.  **AI Assistant Chat:** نافذة دردشة جانبية للتحدث مع "الوكيل" (مثلي) لتوليد الكود أو تصحيح الأخطاء.
2.  **Debugger:** أدوات تتبع الأخطاء (Breakpoints, Step-over, Step-in) مدمجة في المحرر.
3.  **Version Control (Git):** واجهة رسومية لإدارة الإصدارات (Commit, Push, Pull History).
4.  **Deployment Manager:** زر "Run" ذكي يقوم بتثبيت الاعتمادات وتشغيل السيرفر تلقائياً، وزر "Deploy" لنشر التطبيق على السحابة.

## 4. واجهة الهاتف الجوّال (Mobile Interface Adaptation)
يجب أن تكون التجربة متطابقة وظيفياً ولكن محسنة للمس:

- **التخطيط المتجاوب (Responsive Layout):** يتم إخفاء الألواح الجانبية (Sidebar/Preview) خلف قوائم منبثقة أو سحب (Swipe) لتوفير مساحة للمحرر.
- **لوحة مفاتيح المبرمجين (Coding Keyboard):** شريط أدوات إضافي فوق لوحة المفاتيح العادية يحتوي على رموز البرمجة الشائعة ( { } [ ] < > ; / ) وأسهم التنقل لتسهيل الكتابة.
- **التنقل (Navigation):** استخدام "Tabs" سفلية أو علوية للتنقل السريع بين (Code, Console, Preview).
- **اللمس (Touch Actions):** دعم الإيماءات للتحديد والنسخ واللصق الدقيق للنصوص البرمجية.

## 5. البنية التحتية الخلفية للواجهة (Backend Infrastructure)
- **Containerization:** كل مساحة عمل (Workspace) تعمل في حاوية معزولة.
- **Universal Runtime:** دعم تشغيل أي لغة برمجة دون إعدادات معقدة.
- **Multiplayer:** دعم التحرير التعاوني (Real-time Collaboration) حيث يظهر مؤشر كتابة المستخدمين الآخرين في نفس الملف لحظياً (مثل Google Docs).