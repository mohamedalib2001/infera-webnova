🔒 سياسة إلزامية للقوائم الجانبية والتوجيه (Routing Policy)

Mandatory Navigation & Routing Governance Policy

نسخة: 1.0
الحالة: إلزامية – غير قابلة للتجاوز
نطاق التطبيق: جميع القوائم، التوجيهات، الصلاحيات، والصفحات
تاريخ النفاذ: فوري

1. الهدف

تهدف هذه السياسة إلى:

منع تحميل غير ضروري للأكواد

ضمان سرعة المنصة مهما زاد عدد الصفحات

عزل القوائم عن منطق الصفحات

تمكين التوسع الآمن للمنصة (Scalability)

2. مبدأ أساسي غير قابل للنقاش

عرض الصفحة في القائمة ≠ تحميل كود الصفحة

أي خلط بين الاثنين يُعد خطأ معماري جسيم.

3. سياسة القوائم الجانبية (Sidebar Policy)
3.1 القاعدة الذهبية

القائمة الجانبية تعتمد على Metadata فقط
ولا تحتوي على أي منطق أعمال أو استيراد صفحات.

✔️ مسموح في Sidebar:

label

icon

route

permissions / roles

ترتيب العرض

❌ ممنوع تمامًا في Sidebar:

import لأي صفحة

import لأي component ثقيل

API calls

hooks

حسابات أو loops

state معقد

3.2 الشكل الإلزامي لتعريف عناصر القائمة
{
  id: "users",
  label: "إدارة المستخدمين",
  icon: UserIcon,
  route: "/users",
  roles: ["owner", "admin"]
}


أي عنصر لا يلتزم بهذا الشكل يُرفض.

4. سياسة التوجيه (Routing Policy)
4.1 تحميل الصفحات (Lazy Loading)

جميع الصفحات بدون استثناء يجب تحميلها باستخدام Lazy Loading.

const UsersPage = lazy(() => import('@/pages/UsersPage'));


❌ تحميل مباشر = مخالفة جسيمة.

4.2 منع الاستيراد المركزي

❌ يُحظر:

import UsersPage from './UsersPage'
import SettingsPage from './SettingsPage'


✔️ مسموح فقط داخل lazy import.

5. فصل الصلاحيات عن التحميل

الصلاحيات:

تتحكم في الرؤية

لا تتحكم في تحميل الكود

لا يُسمح بتحميل صفحة فقط للتحقق من صلاحية المستخدم.

6. سياسة الحسابات داخل القوائم

أي filtering أو permissions:

يجب أن يكون O(1) أو O(n) صغير

بدون loops متداخلة

ممنوع:

عمليات بحث ثقيلة

معالجة بيانات كبيرة

7. سياسة تعدد الصفحات

عدد الصفحات غير محدود

الأداء لا يتأثر بعدد الصفحات طالما:

لا يتم تحميلها مسبقًا

لا يتم استيرادها في القوائم

يتم الالتزام بالـ lazy loading

8. اختبارات إلزامية

يجب أن تحقق القوائم:

تحميل فوري (< 100ms)

بدون أي API call عند التحميل

بدون re-render غير مبرر

أي إخفاق = منع النشر.

9. مخالفات شائعة (مرفوضة)

❌ Sidebar يستورد صفحات
❌ Sidebar يحتوي Hooks
❌ Route بدون lazy
❌ Permissions تؤدي لتحميل كود
❌ Menu مبني على Components بدل Metadata

10. قاعدة ذهبية ختامية

القائمة الجانبية = خريطة
الصفحة = وجهة
لا يجب أبدًا تحميل الوجهة لرسم الخريطة

11. التنفيذ والمساءلة

هذه السياسة إلزامية

أي مخالفة:

تؤدي لرفض الدمج

أو إعادة هيكلة فورية

لا يُسمح بأي استثناء تقني أو زمني

✅ اعتماد

تم اعتماد هذه السياسة كجزء مكمل لسياسات الأداء والهندسة المعمارية للمنصة.