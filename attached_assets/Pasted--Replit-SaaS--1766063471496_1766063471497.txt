📩 التوجيه التنفيذي النهائي إلى Replit
الموضوع: تنفيذ نظام ربط نطاقات ديناميكي لمنصة SaaS متعددة المستأجرين

نعمل على منصة رقمية بنموذج SaaS متعددة المستأجرين (Multi-Tenant)، ونرغب في تنفيذ نظام احترافي لربط النطاقات (Custom Domains) بشكل ديناميكي يتيح لكل مشترك ربط نطاقه الخاص بالمنصة بسهولة، وبشكل آمن، وقابل للتوسع.

المطلوب منكم

تنفيذ حل عملي قابل للإنتاج (Production-Ready) بناءً على المتطلبات التالية، مع التركيز على البساطة التنفيذية، الأمان، وقابلية التوسع.

1️⃣ المتطلبات الوظيفية (Functional Requirements)
1.1 ربط النطاقات

تمكين المشترك من:

إضافة نطاقه الخاص من لوحة التحكم.

إدارة (إضافة / إزالة) النطاقات.

منع أي تعارض بين نطاقات المشتركين.

1.2 التحقق من ملكية النطاق

اعتماد DNS TXT Record كطريقة أساسية (Primary).

دعم طرق إضافية (اختيارية):

HTML File Verification

Meta Tag Verification

التحقق يجب أن يكون:

مؤتمتًا بالكامل.

مع إعادة المحاولة تلقائيًا (Retry).

2️⃣ إدارة SSL (إلزامي)

استخدام Let’s Encrypt عبر ACME.

إصدار الشهادة تلقائيًا بعد التحقق.

التجديد التلقائي قبل الانتهاء (Auto-Renewal).

دعم:

dns-01 challenge (أساسي)

http-01 (اختياري)

3️⃣ البنية المعمارية (Architecture Expectations)

دعم Multi-Tenant Isolation بشكل صارم.

توجيه ديناميكي بناءً على اسم النطاق (Host-Based Routing).

عدم تقييد الحل بمزوّد واحد (No Vendor Lock-in).

تصميم معياري (Modular Architecture).

4️⃣ تكامل DNS (Provider-Agnostic)

دعم:

Cloudflare

AWS Route53

Google Cloud DNS

Generic DNS Providers

استخدام Adapter Pattern لتسهيل إضافة مزودين جدد.

دعم الاكتشاف التلقائي لمزوّد DNS عند الإمكان.

5️⃣ تجربة المستخدم (UX)

توفير Wizard واضح داخل لوحة التحكم:

إدخال النطاق

اختيار طريقة التحقق

عرض تعليمات دقيقة

التحقق الآلي

إعداد SSL

تأكيد النجاح

عرض حالة النطاق:

Pending

Verified

SSL Issued

Active

Error (مع سبب واضح)

6️⃣ الأمان (Security Requirements)

منع:

ربط نطاق غير مملوك.

استغلال النظام لرفع نطاقات ضارة.

فرض:

Rate Limiting

Max domains per tenant

تسجيل كامل للتغييرات (Audit Logs).

7️⃣ قابلية التوسع (Scalability)

دعم آلاف النطاقات بدون إعادة تصميم.

بنية جاهزة للتوسع الأفقي (Horizontal Scaling).

فصل:

Domain Management

SSL Management

Routing

8️⃣ خطة التنفيذ المطلوبة منكم

يرجى تقديم وتنفيذ ما يلي:

8.1 مرحلة MVP

DNS TXT Verification

Let’s Encrypt SSL

لوحة تحكم أساسية

دعم مزود DNS واحد على الأقل (Cloudflare أو Generic)

8.2 مرحلة التوسع

دعم عدة مزودين

تحسين UX

Auto Renewal

Monitoring & Alerts

9️⃣ المطلوب تسليمه (Deliverables)

كود جاهز للإنتاج.

تعليمات تشغيل ونشر واضحة.

توثيق:

Architecture Overview

Setup Guide

Security Considerations

API Documentation (إن وُجد).

10️⃣ ملاحظات ختامية مهمة

نفضّل الحلول البسيطة القابلة للتطوير على التعقيد غير الضروري.

يمكن اقتراح تحسينات، لكن الالتزام بالمتطلبات الأساسية أولوية.

الهدف هو بناء ميزة Core للمنصة، وليست تجربة أو PoC.

النتيجة المتوقعة