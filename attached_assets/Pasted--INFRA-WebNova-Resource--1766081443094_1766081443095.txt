📩 التوجيه التنفيذي النهائي
INFRA WebNova – نظام تتبع استهلاك الموارد والتكلفة لكل مستخدم

Resource Usage & Cost Attribution System

🎯 الهدف التنفيذي

إظهار أمام كل مستخدم وأمام المالك:

🔢 كمية استهلاك الموارد (API / AI / Storage / Compute)

💰 التكلفة الفعلية على المالك (Real Cost)

🧾 التكلفة المحتسبة على المستخدم (Billed Cost)

📊 الفارق (Margin / Subsidy)

📈 الاستخدام اليومي / الشهري

🚨 تنبيهات عند تجاوز الحدود

❗ بدون تخمين
❗ بدون أرقام تقريبية
❗ بدون اعتماد على مزود خارجي

🧠 1️⃣ المفهوم الأساسي (قاعدة سيادية)

كل طلب = وحدة استهلاك
كل وحدة = تكلفة حقيقية
كل تكلفة = تُنسب لمستخدم واحد فقط

🧱 2️⃣ نموذج بيانات الاستهلاك (Mandatory DB Schema)
2.1 سجل الاستهلاك (Resource Usage Ledger)
interface ResourceUsage {
  id: string;

  userId: string;
  platformId: string;

  resourceType:
    | 'AI_TOKENS'
    | 'API_REQUEST'
    | 'STORAGE_MB'
    | 'COMPUTE_SECONDS'
    | 'PAYMENT_TX'
    | 'BANDWIDTH_MB';

  provider: string; // openai, claude, stripe, etc
  service: string;  // gpt-4, embeddings, webhook, etc

  quantity: number;        // عدد الوحدات
  unitCostUSD: number;     // التكلفة الفعلية للوحدة
  realCostUSD: number;     // التكلفة الحقيقية على المالك

  billedCostUSD: number;   // التكلفة المحتسبة على المستخدم
  pricingModel: 'FREE' | 'FIXED' | 'MARKUP' | 'SUBSIDIZED';

  requestId?: string;

  timestamp: Date;
}

💰 3️⃣ محرك التسعير (Pricing Engine)
3.1 معادلة التسعير القياسية
billedCost = realCost * markupFactor

3.2 نماذج التسعير المدعومة

FREE (المالك يتحمل)

FIXED (باقة)

MARKUP (هامش ربح)

SUBSIDIZED (دعم جزئي)

مثال:
المورد	التكلفة الحقيقية	المحسوبة
GPT-4 Tokens	$0.012	$0.018
Storage	$0.10	$0.10
API Call	$0.001	$0.002
🔐 4️⃣ ربط الاستهلاك بالمستخدم (Critical)

في كل Middleware:

context.usageTracker.track({
  userId,
  resourceType,
  provider,
  service,
  quantity
});


❗ لا طلب يمر بدون تسجيل
❗ حتى الطلبات الفاشلة تُسجّل

📊 5️⃣ واجهة المستخدم – صفحة إدارة المستخدمين
أمام كل مستخدم (OWNER VIEW):
👤 المستخدم: MOHAMED ALI
━━━━━━━━━━━━━━━━━━━━━━
📊 الاستهلاك هذا الشهر:
- AI Tokens: 124,500
- API Requests: 3,200
- Storage: 4.6 GB

💰 التكلفة:
- التكلفة الحقيقية: $23.40
- المحسوبة عليه: $31.20
- الفرق (هامش): +$7.80

📈 الحالة:
- ضمن الحد ✅
- آخر استخدام: قبل 5 دقائق

أزرار إضافية:

📊 تفاصيل الاستهلاك

🚨 تعيين حدود

⛔ إيقاف عند التجاوز

📄 تقرير PDF

📈 6️⃣ لوحة المالك (Owner Dashboard)
مؤشرات فورية

إجمالي تكلفة اليوم

أعلى 5 مستخدمين استهلاكًا

المستخدمون الخاسرون (تكلفة > تحصيل)

الربحية لكل خدمة

جدول مركزي
المستخدم	التكلفة	المحسوبة	الفرق	الحالة
🚨 7️⃣ نظام الحدود والتنبيهات
حدود لكل مستخدم
{
  monthlyLimitUSD: 50,
  autoSuspend: true,
  notifyAt: 80%
}

إجراءات تلقائية:

🔔 تنبيه

⛔ إيقاف المستخدم

🔄 تخفيض الجودة (مثلاً GPT-4 → GPT-3.5)

🧩 8️⃣ التكامل مع أنظمة أخرى

عند إيقاف المستخدم:

❌ إيقاف الاستهلاك

❌ تعطيل مفاتيح API

❌ منع الدفع

❌ تعليق الاشتراك

📜 9️⃣ التقارير (Reports)

تقرير مستخدم فردي

تقرير شهري للمالك

CSV / PDF

مقارنة تكلفة / ربح

🔒 10️⃣ الأمان والشفافية

لا يمكن للمستخدم رؤية تكلفة الآخرين

المالك يرى الكل

كل الأرقام قابلة للتدقيق

لا تعديل يدوي على السجلات

✅ 11️⃣ تعريف النجاح النهائي

النظام مكتمل إذا:

✔️ كل مستخدم له استهلاك واضح

✔️ التكلفة الحقيقية محسوبة بدقة

✔️ الفرق واضح للمالك

✔️ يمكن الإيقاف التلقائي

✔️ لا تطوير لاحق

🏁 الخلاصة

الآن INFRA WebNova ليست مجرد منصة
بل نظام تشغيل مالي وتشغيلي ذكي

المالك:

يعرف من يستهلك

يعرف كم يدفع

يعرف كم يربح

ويتحكم فورًا